package இயேரி.தமிழிசை;

import com.google.common.base.Strings;

public class பயன்பாடுகள் {
    static void அச்சு(Object மாறி) {
        System.out.println(மாறி);
    }

    static boolean வெற்றிடமா(String மாறி) {
        return Strings.isNullOrEmpty(மாறி);
    }

}
